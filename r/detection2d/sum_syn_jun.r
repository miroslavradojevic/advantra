root_folder <- "~/vary_p";

list_root = list.files(root_folder);

for (i in 1:length(list_root)) {
  
  data_dir <- paste(root_folder,list_root[i], sep="/");
  print(data_dir);
  
  list_data_dir <- ;
  
  
} 
